# Erlaubt Post-Install-Build-Skripte der noetigen Pakete.
# (In pnpm 9.12+ ist onlyBuiltDependencies aus package.json verschoben;
#  hier in .npmrc ist es ohne Workspace-Semantik wirksam.)
enable-pre-post-scripts=true
